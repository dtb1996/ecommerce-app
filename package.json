{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:secrets": "concurrently \"npm run dev --prefix backend\" \"npm run dev:secrets --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
